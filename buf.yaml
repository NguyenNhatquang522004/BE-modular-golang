version: v2
modules:
  - path: api/proto                # Trỏ vào thư mục cha của v1
    name: buf.build/quang/social   # Tên định danh (bắt buộc trong v2)
lint:
  use:
    - STANDARD
breaking:
  use:
    - FILE